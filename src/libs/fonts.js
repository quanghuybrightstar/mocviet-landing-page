import { Inter, Calistoga } from "next/font/google";

export const interFont = Inter({ subsets: ["latin"], display: "swap" });

export const calistogaFont = Calistoga({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
});
